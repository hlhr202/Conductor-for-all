import { SkillsGenerator } from '../types.js';

export class ClaudeCodeSkillsGenerator implements SkillsGenerator {
  async generate(targetDir: string): Promise<void> {
    // To be implemented
  }
}
